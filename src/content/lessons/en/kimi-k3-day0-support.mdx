---
title: "Kimi K3: Architecture and SGLang Day-0 Support"
description: "How Kimi K3 uses KDA, AttnRes, and LatentMoE to support 2.8T parameters and 1M-token context—and how SGLang serves the hybrid architecture with unified memory, chunked PP, and DCP."
order: 2
concepts:
  - Kimi Delta Attention
  - 3:1 interleave & NoPE
  - Attention Residual
  - Stable LatentMoE
  - unified memory pool
  - chunked PP / DCP
tryOnSglang: "https://github.com/sgl-project/sglang"
references:
  - label: "Kimi K3 Technical Report"
    url: "https://arxiv.org/abs/2607.24653"
  - label: "LMSYS Blog: Kimi K3 Day-0 Support"
    url: "https://www.lmsys.org/blog/2026-07-27-kimi-k3-day0-support/"
  - label: "Kimi Linear: An Expressive, Efficient Attention Architecture"
    url: "https://arxiv.org/abs/2510.26692"
  - label: "Attention Residuals Technical Report"
    url: "https://arxiv.org/abs/2603.15031"
---

import ArchViz from "../../../viz/kimi-k3-day0/ArchViz";
import CacheViz from "../../../viz/kimi-k3-day0/CacheViz";
import MhaViz from "../../../viz/kimi-k3-day0/MhaViz";
import StateChannelViz from "../../../viz/kimi-k3-day0/StateChannelViz";
import LinearFlowViz from "../../../viz/kimi-k3-day0/LinearFlowViz";
import DeltaRuleViz from "../../../viz/kimi-k3-day0/DeltaRuleViz";
import KeyOverlapViz from "../../../viz/kimi-k3-day0/KeyOverlapViz";
import KdaGateViz from "../../../viz/kimi-k3-day0/KdaGateViz";
import AttnResViz from "../../../viz/kimi-k3-day0/AttnResViz";
import MoeViz from "../../../viz/kimi-k3-day0/MoeViz";
import RadixStateViz from "../../../viz/kimi-k3-day0/RadixStateViz";
import PoolViz from "../../../viz/kimi-k3-day0/PoolViz";
import PipelineViz from "../../../viz/kimi-k3-day0/PipelineViz";
import DcpViz from "../../../viz/kimi-k3-day0/DcpViz";
import ServingPerfViz from "../../../viz/kimi-k3-day0/ServingPerfViz";

## The Kimi K3 architecture

K3 is a 2.8T-parameter MoE model with 104B parameters active per token. It
supports a native 1M-token context. Its MoE expert weights ship in MXFP4, with
MXFP8 activations. K3 has 93 layers: 69 use KDA linear attention and 24 use
global MLA. Every attention layer is paired with a Stable LatentMoE FFN, while
Attention Residual aggregates representations across blocks. Click the diagram
for the complete structure:

<ArchViz client:visible lang="en" />

### KV cache at 1M tokens

Start with memory. After MLA compression, K3's 24 MLA layers need about 27KB
of KV cache per token in total. If all 93 layers used MLA, one 1M-token request
would need roughly 110GB; uncompressed MHA would cost another order of
magnitude. K3 therefore cannot keep context-growing softmax attention in most
layers.

K3 interleaves 3 KDA + 1 MLA. The KDA recurrent state has a fixed size, about
0.4GB per request across the machine, and updates in place each step. MLA keeps
global attention. The model uses no RoPE (NoPE); position comes implicitly from
the KDA recurrence:

<CacheViz client:visible lang="en" />

At 1M tokens, the 24 MLA layers need about 28GB, or 1/3.9 of the all-MLA
hypothetical. Compute follows the same pattern: softmax attention scans the
history for every decoded token (`O(N)`), while KDA updates a fixed-size state
(`O(1)`).

### MHA: scan the whole history each step

First look at what KDA replaces. In ordinary MHA, token `N` reads the preceding
`N−1` cached tokens in three steps:

1. **Score:** compare against each earlier token `i` with `z[N,i] = dot(q[N], k[i]) / sqrt(d_k)`.
2. **Normalize:** compute `Z = Σⱼ₌₁…N−1 exp(z[N,j])`, then assign attention weight `a[N,i] = exp(z[N,i]) / Z`.
3. **Aggregate:** take the weighted sum of values, `o[N] = Σᵢ₌₁…N−1 a[N,i]v[i]`.

A length-`N` sequence therefore performs about `N²/2` dot products: `O(N²)`
compute and `O(N)` cache. In the diagram, "it" can place its weight directly on
"cat" five tokens earlier:

<MhaViz client:visible lang="en" />

Softmax attention buys direct, per-position reads. The cost is rescanning the
entire history for every new token.

### Linear attention: a fixed-size state

Linear attention no longer keeps one KV entry per token. It accumulates history
into a fixed-size matrix, `S = Σᵢ k[i]v[i]^T`. Think of S as associative
memory: k selects a write direction, v is the content, and q reads with
`o = S^Tq`. No matter how long the context becomes, each step reads and writes
only S.

<details className="lesson-details">
  <summary>Math details: heads and channels inside the 128×128 state</summary>
  <p>K3 has hidden size <code>7168</code>. Its Q/K/V projections produce <code>96</code> KDA heads. Fix one head and omit its index: <code>k[t], v[t] ∈ R^128</code>, with state matrix <code>S[t] ∈ R^(128×128)</code>. These heads are learned feature spaces, not literal slices of the 7168 input coordinates.</p>
  <p>Within this head, <code>ch[j] = k[t][j]</code> is key-feature dimension <code>j</code>. Increasing <code>d_k</code> adds key-feature axes; increasing <code>d_v</code> widens the value stored along each axis. State and update cost per head grow as <code>d_k×d_v</code>. K3 sets both to 128, giving 16,384 state scalars per head.</p>
</details>

<StateChannelViz client:visible lang="en" />

Now decode the same sentence used in the MHA diagram:

<LinearFlowViz client:visible lang="en" />

In MHA, "it" can point directly to "cat" because every historical token has its
own KV position. The linear-attention state S has no token-position axis, so its
readout mixes all historical writes in one fixed state. The diagram above shows
the simplest form of naive linear attention: plain accumulation.

### DeltaNet: adding a delete operation to the fixed state

Naive linear attention keeps adding to its state but cannot overwrite an old
value. If the same key direction first receives 1 and then 4, both remain in S;
in this simplified example, the next read returns `1+4`.

DeltaNet first reads the old value, `r_old = S[t−1]^T k[t]`, then writes only
the difference: `S[t] = S[t−1] + β[t]k[t](v[t] − r_old)^T`. For a normalized
key, the new readout is `r_new = (1−β[t])r_old + β[t]v[t]`.

The coefficient `β[t]` controls rewrite strength: `β[t]=1` fully replaces the
old association, smaller `β[t]` retains more of the old value, and `β[t]=0`
does not rewrite it at all.

<DeltaRuleViz client:visible lang="en" />

This fixes same-direction rewriting, but not capacity: as unrelated history
enters the fixed-size S, what should decay? Real key vectors are also not
perfectly orthogonal, so a write in one direction can project into another and
create crosstalk.

<details className="lesson-details">
  <summary>Math details: A/B in the diagram and the crosstalk term</summary>
  <p>A and B are full directions in key space, not token slots or individual channels. <code>A=1</code> is shorthand for “k points along A and v represents 1.” Colors trace the contribution from each write; the real S stores only their sum.</p>
  <p>If <code>q[A]=k[A]</code> and S contains both <code>k[A]v[A]^T</code> and <code>k[B]v[B]^T</code>, then <code>o[A] = S^Tq[A] = v[A] + dot(k[A],k[B])v[B]</code>. The second term is B leaking into A, scaled by the inner product of the two keys.</p>
</details>

<KeyOverlapViz client:visible lang="en" />

### KDA: delta rule with per-channel gating

The fixed state does not grow with context—that is why it saves memory—but more
and more history must share the same S. The delta rule can rewrite the same or a
nearby key direction, but it does not proactively clear every irrelevant old
signal. KDA therefore adds channel-wise gating. For every token and head, the
model dynamically produces a vector `α[t]` from the current hidden state,
applies `Diag(α[t])` to scale the old state's channels, and
then performs the DeltaNet rewrite. `α[t]` is not a fixed learned matrix:
the projection that produces it is learned, while the actual retention values
depend on the current input.

<details className="lesson-details">
  <summary>Math details: how KDA gates and rewrites the same S</summary>
  <p>The full update is <code>S[t] = (I−β[t]k[t]k[t]^T) Diag(α[t]) S[t−1] + β[t]k[t]v[t]^T</code>, where <code>α[t] ∈ (0,1)^(d_k)</code> and each entry scales one key channel of S. It does not select A or B directly or rotate keys into orthogonal directions; every historical contribution in a row is scaled together.</p>
  <p>The diagram reduces the real state to 2D, with <code>k[A]=(1/√2)[1,1]^T</code> and <code>k[B]=(1/√2)[1,−1]^T</code>. Both A and B span <code>ch[1]</code> and <code>ch[2]</code>. The gate scales both rows, then the delta rule writes the residual needed to reach target value 4 along the full <code>k[A]</code>.</p>
</details>

<KdaGateViz client:visible lang="en" />

The Kimi Linear paper also interprets the gate as learned position encoding in
place of RoPE's fixed rotation. Its chunkwise kernel for the constrained DPLR
form is about twice as fast as the general implementation, making 69 KDA layers
practical.

### Attention Residual

An ordinary Transformer's residual stream is like one draft passed from the
bottom layer to the top, with every layer editing the same page. This keeps
information moving upward, but by layer 90, shallow details are mixed with
dozens of later updates; a deep layer cannot reopen an earlier version directly.

Attention Residual keeps a few historical versions on the side. K3 preserves
the ordinary residual inside each group and saves one block output every 12
layers. A later block does not have to rely only on the current draft: a learned
pseudo-query weights the embedding and saved summaries, then mixes back the
representations most useful at that moment:

<AttnResViz client:visible lang="en" />

Shallow information no longer has to survive unchanged in the residual stream,
and different deep blocks can retrieve different historical representations.

<details className="lesson-details">
  <summary>Math and cost: why K3 saves one summary every 12 layers</summary>
  <p>For a later block ℓ, the pseudo-query produces weights <code>α[ℓ,i]</code> and retrieves <code>r[ℓ] = Σ[i&lt;ℓ] α[ℓ,i]v[i]</code>. This is a softmax-weighted mixture of the embedding and earlier block summaries, not a hard choice of one layer.</p>
  <p>Full AttnRes saves every layer output and gives the finest retrieval, but costs more memory, bandwidth, and pipeline communication. Block AttnRes saves one summary per group, reducing state from <code>O(Ld)</code> to <code>O(Nd)</code>. K3 uses one summary every 12 layers. In the optimized implementation, typical inference-latency overhead is below 2%, while training wall-clock overhead under pipeline parallelism is below 4%.</p>
</details>

### LatentMoE

Every K3 FFN uses MoE: each token selects 16 of 896 routed experts, while 2
shared experts are always active. The routed-pool selection rate is therefore
`16/896 ≈ 1.8%`; the separate whole-model ratio is
`104B/2.8T ≈ 3.7%` active
parameters per token.

Routing and expert compute project the 7168-d hidden state down to a 3584-d
latent space, then project back to full width. This halves both the 16-way
all-to-all dispatch traffic and expert weight volume:

<MoeViz lang="en" />

High sparsity makes training harder to stabilize. K3 uses RMSNorm, bounded
SiTU-GLU, and Quantile Balancing, which derives expert quotas directly from
router-score quantiles.

At this point, we have seen how KDA, MLA, Attention Residual, and LatentMoE form
K3's hybrid architecture. But the model design only answers how to compute. In
production, SGLang must also decide how to place these different states in
memory, parallelize prefill, and scale decode. Those are the three questions in
the next half.

## SGLang's day-0 adaptations

K3's hybrid architecture creates two inference states with completely
different lifecycles:

| | MLA (24 layers) | KDA (69 layers) |
| --- | --- | --- |
| State shape | KV cache, appends per token | Recurrent state, fixed size |
| Growth | ~27KB per token, append-only | ~54MB per request (TP=8), overwritten in place each step |

MLA appends KV per token, while KDA reserves a fixed amount per request and
overwrites it every step. That difference propagates into memory allocation,
prefill parallelism, and decode scaling—the three parts of SGLang's day-0 work.

### RadixAttention: prefix caching for mutable KDA state

Traditional RadixAttention can share a prefix across requests because attention
KV is append-only and never changes. KDA's recurrent state `S` is different: it
is overwritten in place after every token. Suppose D and E both hit the prefix
ABC. If they directly shared the same `S(ABC)`, D's next step would turn it into
`S(ABCD)`, so E could no longer start from the state after ABC.

SGLang therefore never lets a request mutate the shared state in the radix
tree. A KDA state stored in the tree is a read-only checkpoint. After a prefix
hit, copy-on-write restores it into the request's private working slot, and the
forward pass mutates only that copy. The visualization first compares how KDA
and MLA grow under two workload changes, then steps through copy-on-write,
snapshot, donate, and checkpoint eviction:

<RadixStateViz client:visible lang="en" />

This does not retain one permanent copy of `S` per token. Checkpoints are placed
only at aligned radix nodes, prefill chunk boundaries, and fixed decode
intervals, then bounded by a per-path cap and LRU. If a request diverges in the
middle of a radix edge, SGLang restores the nearest checkpoint above, replays a
short suffix forward, and plants a checkpoint at the new aligned fork.

### Memory management for two state types

The old approach preallocates separate KDA and MLA regions at startup. KDA
demand follows concurrency, while MLA demand follows total context length. If
the workload differs from the estimate, one region fills while the other sits
idle. SGLang puts both in one pool: fixed KDA blocks allocate from the left,
MLA pages from the right, and both share the free space in the middle:

<PoolViz client:visible lang="en" />

In the baseline, allocations pick free slots inside two statically sized regions.
When the MLA region fills, free KDA slots cannot be borrowed. The unified layout
instead packs KDA from the left and MLA from the right, so every request keeps running.

### Chunked pipeline prefill

Prefill processes many prompt tokens at once, providing enough chunks to fill a
pipeline. TP8 keeps all eight GPUs in lockstep at every layer, pays an AllReduce
per layer, and narrows each GEMM. Chunked PP8 instead assigns ranges of the 93
layers to G1–G8 and lets different GPUs process different chunks. GPUs pass only
the activation from the preceding stage, allowing P2P transfers to overlap
with computation on the next chunk:

<PipelineViz client:visible lang="en" />

This removes per-layer AllReduce, restores full-width GEMMs, and hides 91% of
inter-stage P2P. In an 8K-prefill comparison on 2×4 GB300, a representative
PP8×TP1 point reaches 5,958 input tok/s/GPU, 1.64× TEP8. Across configurations,
prefill capacity improves by 1.45–1.72×.

Decode usually has only one new token per step, not enough work to fill a deep
pipeline, and it is more sensitive to step latency. TP8 remains the better fit.
A common PD-disaggregated split is `PP8 prefill → TP8 / DCP8 decode`. PP still
pays fill and drain bubbles, so it does not win with too few requests or chunks.

### Decode context parallelism (DCP)

MLA has multiple attention heads, but they all share the same compressed KV latent,
leaving no cache head axis for TP to shard. Every TP8 rank therefore replicates the
complete MLA KV latent, and adding GPUs does not increase logical context capacity.
DCP shards by token position instead: each GPU computes partial attention over its local `1/N` KV,
then one packed all-to-all merges the result exactly:

<DcpViz client:visible lang="en" />

DCP primarily increases capacity and concurrency, not latency for one short
request. Keeping more long sessions on the GPU avoids host offload, re-prefill,
and throughput collapse. On K3, DCP8 raises logical capacity from 1.5M to
12.2M tokens (7.9×) and reaches 541 tok/s at 48 agent sessions. It costs one
all-to-all per MLA layer. KDA state has no token-position axis and remains
TP/head-sharded.

### Performance numbers: single-request decode and the serving frontier

BS=1 decode, aggregate system throughput, and per-user speed are three distinct
metrics. The left side follows 15 kernel and communication optimizations that
raise non-speculative BS=1 decode from 44.3 to 112.5 tok/s, then marks DSpark's
separate ~423 tok/s result. The right side shows the PD-disaggregated serving
frontier between aggregate throughput and per-user speed.

<ServingPerfViz client:visible lang="en" />

## Acknowledgments

Thanks for reading. We hope these interactive diagrams make both K3's
architectural tradeoffs and their consequences for SGLang's memory management
and parallelism easier to see.

Kimi K3 day-0 support was a collaboration between the SGLang & Miles team at
RadixArk and the Moonshot AI team, together with NVIDIA, AMD, Approaching AI,
Baseten, and Modal.

- **AMD:** Wun-guo Huang, Xinyi Song, Hai Xiao, Soga Lin, Duyi Wang, Thomas Wang
- **Approaching AI:** Huanming Shen, Xiaohao Zhang, Nan Li, Mingxing Zhang

Thanks to DigitalOcean for providing AMD instances for testing. Thanks also to
Google Cloud, DigitalOcean, Nebius, fal, RunPod, DeepInfra, and GMI Cloud for
serving Kimi K3 on SGLang.
