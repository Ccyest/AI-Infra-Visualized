---
title: "Unified Memory in SGLang: retain 1.9× as many cached prefixes under pressure"
description: "To support modern hybrid attention models, SGLang saves both per-token KV (for full attention) and recurrent states (for Mamba layers). Giving each a fixed share of GPU memory can fill one while the other still has room, leaving memory unused. Unified Memory lets both share one buffer and allocates space as demand changes. This article explains how SGLang makes that work and the optimization built on it. In a cache-pressure experiment, the eviction improvement retained **1.9×** as many prefixes and reduced the returning requests' total time by **37.9%**."
publishedDate: "2026-09-09"
updatedDate: "2026-09-23"
order: 7
concepts: []
tryOnSglang: "https://github.com/sgl-project/sglang"
references:
  - label: "LMSYS Blog: SGLang and Miles Add Day-0 Support for Kimi K3"
    url: "https://www.lmsys.org/blog/2026-07-27-kimi-k3-day0-support"
  - label: "SGLang Cookbook: Kimi K3"
    url: "https://docs.sglang.io/cookbook/autoregressive/Moonshotai/Kimi-K3"
  - label: "sglang#29678: feat(mem_cache): unified memory pool for hybrid Mamba / SWA models"
    url: "https://github.com/sgl-project/sglang/pull/29678"
  - label: "sglang#33091: [unified-memory] Stop eviction when shared allocation capacity is sufficient"
    url: "https://github.com/sgl-project/sglang/pull/33091"
  - label: "sglang#35177: feat(unified-memory): three sub-pools for mamba + hybrid-SWA models"
    url: "https://github.com/sgl-project/sglang/pull/35177"
---

import StateGrowthViz from "../../../viz/unified-memory/StateGrowthViz";
import CapacityViz from "../../../viz/unified-memory/CapacityViz";
import PageLayoutViz from "../../../viz/unified-memory/PageLayoutViz";
import CompactionViz from "../../../viz/unified-memory/CompactionViz";
import EvictionViz from "../../../viz/unified-memory/EvictionViz";
import BenchmarkViz from "../../../viz/unified-memory/BenchmarkViz";

> Prerequisite: read [Unified Radix Cache](../unified-radix-cache/).

## Background

During inference, a model saves keys and values, or KV, for use when generating the answer. The KV cache is the container that holds this KV. Modern hybrid models have different kinds of attention layers, and they save this history in different ways:

- **Full-attention layers keep keys and values for each token.** Reading more tokens adds more KV. During generation, attention reads this KV to look back at the document.
- **Recurrent layers update a fixed-size state.** Each new token is written into that state, overwriting its earlier contents: the contents change, but the state does not grow. Because of this overwriting, a state can be reused only if it was saved at exactly the same position; otherwise the history it held has already been overwritten. Each active request needs its own working state. Keeping a copy of that state for later reuse, called a checkpoint, takes another state slot.

The figure follows one request. As its context grows, KV accumulates while the recurrent state is updated in place. A hybrid model needs both to continue generating.

<StateGrowthViz client:visible lang="en" />

This creates a sizing problem. Many short requests need many state slots but little KV per request. A few long requests need fewer working states but much more KV. If SGLang reserves a fixed amount of memory for each kind at startup, one pool can fill up while the other still has unused space, so no fixed split handles both cases.

## Unified Memory

### Share the unused space

SGLang's Unified Memory puts both kinds of data in **one GPU buffer**. Recurrent states grow from one end; KV grows from the other. Either side can take bytes from the free region between them. The total buffer size stays the same, while the capacity available to each kind changes with demand.

Suppose that buffer has 24 units. Each request needs 2 units of recurrent state and 1 unit of KV. A fixed split of 8 units for states and 16 for KV admits only four requests: the state pool is full, even though 12 KV units are unused. The shared pool fits all seven requests in the example, using 21 units.

<CapacityViz client:visible lang="en" />

Long contexts can cause the opposite problem: KV runs out while state capacity sits idle. Select “Long contexts” to see that case. Sharing helps when the fixed split is the limit. When the combined data fills the whole buffer, requests still have to wait or cached data must be evicted.

Here, “Unified Memory” means sharing capacity between SGLang's caches on the same GPU. It does not move data to CPU memory. [HiCache](../hicache/) handles that separate task by saving cached history in CPU memory or external storage and loading it back when needed.

### Pack data into movable blocks

The shared buffer is one continuous range of GPU memory. The allocator hands out space from its two ends. It also has to return freed space to the free region in the middle, and as shown below, it sometimes moves blocks to close holes. All of this requires each block it manages to occupy one continuous range.

KV is not stored that way by default. SGLang allocates KV in pages, each holding several tokens, and every full-attention layer keeps its own KV for those tokens. Normally each layer has a separate buffer, so one page is split into one piece per layer, spread across those buffers. Releasing the page leaves a small hole in every layer's buffer, so the freed space stays scattered across the separate layer buffers.

Unified Memory therefore switches to a page-major layout. All layers' KV for one page sits side by side in one contiguous block, called a **page envelope**. The data in the page is unchanged; only its position in memory differs. Releasing the page now frees one continuous range, which can return to the shared free region or be used for recurrent state. Moving the page copies one block instead of one piece per layer.

Does this slow down GPU computation? Attention works on one layer at a time and reads that layer's K and V page by page. As long as each piece it reads is contiguous, the GPU can load it in large chunks. Both layouts provide this. In a page envelope, layer 1's K for page 1 is still one contiguous block; it just sits farther from the same layer's block in the next page. Paged attention already reads one page at a time through a page table, and a request's pages were never guaranteed to be adjacent, so reads stay essentially as efficient. SGLang builds a strided view for each layer over the shared buffer, so kernels still see one K/V tensor per layer, and it converts each token index to its position inside the envelope.

The costs are elsewhere:

- Kernels must accept these strided views, so Unified Memory currently supports only some attention backends and requires K and V heads of the same size.
- Some recurrent-layer prefill kernels accept only contiguous state. SGLang copies the batch's states into a contiguous buffer, runs the kernel, then writes the results back.
- Each forward pass translates virtual IDs into physical positions.

Results at the end of this article reports the measured overhead.

The layer-major GPU layout described in [HiCache](../hicache/) is the default; GPU KV becomes page-major only when Unified Memory is enabled. HiCache's page-major host layout is a separate choice in CPU memory, made so that host memory and storage can exchange whole pages.

The figure uses three layers and two pages. Select “Release page 1”: separate layer buffers are left with three scattered holes, while page envelopes leave one continuous free range.

<PageLayoutViz client:visible lang="en" />

A recurrent-state block similarly holds the state needed across the recurrent layers. KV pages and state blocks keep their own sizes. Both draw from the same buffer, so freeing a large state block can make room for several smaller KV pages.

### Consolidate freed space at runtime

In Unified Memory, a freed block may sit between two live blocks. In the figure, state blocks A, B and C occupy the left end. Releasing B leaves a hole, but C still separates that hole from the shared free region, so the freed space cannot be fully used. SGLang therefore moves C into B's old location. C's old space then joins the free region, where new KV pages can be allocated.

Requests still need to find C after it moves. SGLang gives C a stable **virtual ID** and keeps a table of its current location. The request continues to refer to C; the table changes from “C is at 8” to “C is at 4”. Moving the data does not require rewriting every request that refers to it.

<CompactionViz client:visible lang="en" />

This process is called **compaction**. SGLang can leave a hole temporarily and compact when an allocation needs the space. It must wait until moving the data is safe, then prepare GPU reads and writes with the updated locations. Data still in use by GPU computation or a network transfer cannot be moved underneath that operation.

The [shared-pool implementation](https://github.com/sgl-project/sglang/pull/29678) introduced this allocation and mapping mechanism. A [later extension](https://github.com/sgl-project/sglang/pull/35177) lets three kinds share the buffer: full-attention KV, sliding-window KV and recurrent state. Sliding-window layers only need a recent window of tokens; their capacity demand can differ from both of the other pools.

## Keep more reusable prefixes

SGLang can keep a document's KV and a recurrent-state checkpoint after a request finishes. When another request starts with exactly the same token prefix, it can reuse that saved history and compute only the new suffix. Evicting the cached prefix frees memory now, but a later request may have to compute it again.

With shared memory, the eviction decision must count all the capacity it can reclaim. Suppose a new allocation needs 6 units of KV space. Evicting one cached prefix releases 1 unit of KV and a 2-unit state checkpoint. After two evictions and any needed compaction, 6 units are available. Counting only the 2 KV units would make SGLang keep deleting prefixes even though the allocation already fits.

<EvictionViz client:visible lang="en" />

SGLang's [eviction improvement](https://github.com/sgl-project/sglang/pull/33091) checks whether the shared allocator can satisfy the pending allocation after each eviction. Once it can, eviction stops. The check includes space recoverable from the other pool, so more useful prefixes can remain cached.

## Results

The experiment below first caches 28 different prefixes, then sends an unrelated long request to create memory pressure. Finally, it sends the original prefixes again. A surviving prefix saves repeated prompt computation; an evicted one must be recomputed.

**Both variants already use Unified Memory.** The comparison changes when allocation-driven eviction stops. With the improved rule, more prefixes survive, and the 28 returning requests finish sooner.

<BenchmarkViz client:visible lang="en" />

The experiment limits the pool to 8,192 full-attention tokens and 32 recurrent-state slots, with an 8,192-token prefill chunk. With smaller 2,048-token chunks, both variants retain 17 of the 28 prefixes. The improvement depends on whether freeing state capacity can cover the pending KV allocation; this experiment did not show a stable increase in overall throughput.

Sharing capacity also costs work: SGLang maintains location mappings, sometimes copies blocks and pays the layout costs described above. In the [three-pool evaluation](https://github.com/sgl-project/sglang/pull/35177), median time between output tokens changed by −0.81% to +1.32% across the tested models at matched attention backends. These figures measure generation; the evaluation did not separately report the cost of the prefill state copy. The benefit is better use of available memory when demand shifts between pools. It does not make every GPU computation faster.
