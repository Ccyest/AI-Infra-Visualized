---
title: "HiCache in SGLang: hybrid-model caching with up to 15.5× input throughput"
description: "SGLang reuses cached prefixes to avoid repeated prefill, but long conversations soon outgrow GPU memory. HiCache retains that history in CPU memory and external storage, then overlaps transfers with computation to make reuse worthwhile. As models gain recurrent layers and speculative drafts, it must also recover every state needed to continue from the same prefix. This lesson follows those design changes and the tradeoff between host caching and temporary buffering. In a DeepSeek-V4-Flash multi-turn benchmark, adding Mooncake L3 brings effective input throughput to **15.5×** the GPU-only baseline, with cache hits near **98%**."
publishedDate: "2026-09-10"
updatedDate: "2026-09-11"
order: 8
concepts: []
tryOnSglang: "https://github.com/sgl-project/sglang"
references:
  - label: "LMSYS Blog: SGLang HiCache: Fast Hierarchical KV Caching with Your Favorite Storage Backends"
    url: "https://www.lmsys.org/blog/2025-09-10-sglang-hicache/"
  - label: "sglang#8651: Support page first layout  zero copy for mooncake store"
    url: "https://github.com/sgl-project/sglang/pull/8651"
  - label: "sglang#9109: 3fs zerocopy"
    url: "https://github.com/sgl-project/sglang/pull/9109"
  - label: "SGLang Cookbook: HiCache"
    url: "https://sgl-project-sglang-93.mintlify.app/optimization/hicache"
  - label: "SGLang: HiCache benchmarks"
    url: "https://github.com/sgl-project/sglang/tree/main/benchmark/hicache"
  - label: "sglang#2693: Hierarchical Caching for SGLang"
    url: "https://github.com/sgl-project/sglang/pull/2693"
  - label: "sglang#7313: Kernels for efficient KV cache IO"
    url: "https://github.com/sgl-project/sglang/pull/7313"
  - label: "sglang#7704: Hicache Storage Layer Prototype"
    url: "https://github.com/sgl-project/sglang/pull/7704"
  - label: "sglang#21259: [HiCache & HybridModel] mooncake backend support DSA & mamba  model"
    url: "https://github.com/sgl-project/sglang/pull/21259"
  - label: "sglang#30393: [HiCache] Support packed and sidecar draft caches for MTP/EAGLE/DSpark"
    url: "https://github.com/sgl-project/sglang/pull/30393"
  - label: "sglang#33639: [Hicache][2/2]Support Mamba branching in Unified Radix Cache with HiCache"
    url: "https://github.com/sgl-project/sglang/pull/33639"
  - label: "sglang#34798: [HiCache] Buffer-only mode for HiCache host memory layer"
    url: "https://github.com/sgl-project/sglang/pull/34798"
  - label: "sglang#31920: [HiCache] Add model-aware key isolation to Mooncake Store"
    url: "https://github.com/sgl-project/sglang/pull/31920"
  - label: "sglang#37050: docs: state that HiCache L2 is instance-private and only L3 is shared"
    url: "https://github.com/sgl-project/sglang/pull/37050"
  - label: "LMSYS Blog: Unified Radix Cache: One Tree for Hybrid Model Prefix Caching"
    url: "https://www.lmsys.org/blog/2026-08-11-unified-radix-cache/"
---

import CacheTreeViz from "../../../viz/hicache/CacheTreeViz";
import LayoutViz from "../../../viz/hicache/LayoutViz";
import OverlapViz from "../../../viz/hicache/OverlapViz";
import PrefetchViz from "../../../viz/hicache/PrefetchViz";
import ResultsViz from "../../../viz/hicache/ResultsViz";
import TimelineViz from "../../../viz/hicache/TimelineViz";
import StateRestoreViz from "../../../viz/hicache/StateRestoreViz";
import DraftRestoreViz from "../../../viz/hicache/DraftRestoreViz";
import HostModeViz from "../../../viz/hicache/HostModeViz";
import MultiTurnBenchViz from "../../../viz/unified-radix-cache/MultiTurnBenchViz";

## Background

A model computes keys and values, or KV, for the tokens it reads. During generation,
attention uses this KV to look back at the context. If a later request starts with
the same token prefix, SGLang can reuse the corresponding KV and compute only the
new suffix. This saves prefill work and shortens time to first token, or TTFT.

The difficulty is keeping the old KV available. Long conversations take more space
with every turn. More clients add more histories. When the GPU cache fills up,
eviction can remove a prefix just before the next request needs it. A larger cache
helps only if loading the saved KV costs less than computing it again.

## Technical evolution

HiCache first extended GPU caching into CPU memory, then added an external
storage tier. Alongside capacity, it needed transfers cheap enough to beat
recomputation. Hybrid models and speculative decoding later changed what must
be saved: a prefix can require several kinds of state. Buffer-only mode then
separated long-term retention from temporary transfer space. These milestones
mark the design changes explained below.

<TimelineViz client:visible lang="en" />

## HiCache

### Design and mechanism

SGLang HiCache keeps reusable KV across GPU HBM, CPU DRAM, and external storage.
The GPU still performs attention. CPU memory and storage retain KV that would
otherwise be discarded, and the cache controller handles transfers between tiers.

HiRadixTree indexes the local cache by token sequence. Each node holds a token
segment and references to its KV in the GPU and CPU pools. Concatenating the
segments along a path gives the shared prefix. A GPU hit is ready to use; a CPU hit
needs a transfer to newly allocated GPU slots. If the prefix is absent locally,
the controller can query external storage using prefix-derived keys and prefetch
matching pages into CPU memory. A local tree miss does not by itself prove a
storage hit.

<CacheTreeViz client:visible lang="en" />

The diagram plays one serving instance through five requests, using short token
segments and arbitrary pool indices. Prefix A is computed once and backed up.
B survives GPU eviction in CPU memory, then survives CPU eviction in external
storage, and each time the controller restores it to the GPU instead of
recomputing it. The last request shows a local miss that is also a storage
miss, so its suffix is computed as ordinary prefill. Backup copies can coexist
across tiers. Moving KV does not require deleting every copy from the tier it
came from.

### Storage backends

The 2025 design supports Mooncake, DeepSeek 3FS, NIXL, and a local file backend.
The backend supplies key lookup, reads, and writes; the central controller handles
scheduling and synchronization. NIXL is a transfer library that can connect to
other storage systems, so these backends should not be read as four equivalent
storage devices.

## Data movement

### Memory layout

A model consumes KV one layer at a time. GPU memory therefore keeps the
layer-first layout expected by its computation kernels. Storage I/O benefits from
larger contiguous transfers. HiCache gives the host pool a page-first layout,
placing one page's KV for different layers next to each other. The same data has
a different physical order on the two sides.

<LayoutViz client:visible lang="en" />

SGLang uses GPU-assisted I/O kernels for CPU–GPU movement. The blog reports up to
3× the transfer throughput of the standard `cudaMemcpyAsync` path. Page-first
layout together with zero-copy I/O reaches up to 2× higher throughput between
host memory and storage in typical deployments. These are improvements to
different transfer paths, not factors to multiply into an end-to-end speedup.
The Mooncake and 3FS changes are linked in the references.

### Layer-wise loading

A CPU hit does not need to wait for every layer's KV before computation starts.
Once layer 0 is loaded, the GPU can execute it while later layers are still being
transferred. Each layer must wait for its own KV, but it does not need the KV for
all later layers. This overlap can hide part of the transfer time.

<OverlapViz client:visible lang="en" />

The first load still takes time. If transfers are slower than computation,
later layers can still stall. The animation assumes fixed transfer and compute
durations to show the dependency; its timeline is not a hardware benchmark.

## Cache policies

### Prefetch

Storage latency is usually higher and less predictable than CPU–GPU latency.
HiCache starts prefetching into CPU memory while a request waits for scheduling.
Best-effort prefetch can stop when the request is ready to run, using the
available prefix and recomputing what is missing. Waiting longer can preserve
more reuse, but also delays the request. A timeout policy bounds that wait.

<PrefetchViz client:visible lang="en" />

The example treats the requested prefix as one indivisible block. Real requests
can reuse a completed prefix of a larger transfer. With fast storage, both
policies finish the read before scheduling. With a moderately late read,
waiting can avoid a larger recomputation. With very slow storage, waiting loses.
More cache hits alone do not guarantee a lower TTFT.

### Backup

Backing up every cached region spends bandwidth now to improve the chance of
reuse later. HiCache exposes three choices:

| Policy | Backup timing | Main tradeoff |
| --- | --- | --- |
| Write-through | Back up cached KV proactively | More retained data, more I/O |
| Write-through-selective | Use hit counts to back up hot regions | Less I/O, less coverage for cold regions |
| Write-back | Defer backup until eviction from the faster tier | Less early backup traffic, work on the eviction path |

The useful setting depends on transfer bandwidth and capacity in the lower tiers.
Adding storage does not remove those limits.

## Hybrid-model state

Larger caches solve the capacity problem, but they also need to preserve the
right data. The 2026 HiCache work extends the original design to models that
use several kinds of history at once.

### Reuse boundaries

Full attention needs KV for the reusable prefix. Sliding-window attention needs
the trailing window at the reuse boundary. A recurrent layer needs a checkpoint
at that boundary. Its state is updated as tokens arrive; a later checkpoint
cannot simply be sliced to recover an earlier one.

SGLang's Unified Radix Cache keeps these components on the same token tree,
with separate pool references and reuse checks. HiCache must back up and restore
the components needed at the chosen boundary. For example, Qwen3.5 also needs
recurrent state, while DeepSeek-V3.2 needs its sparse-attention indexer buffers.
Copying ordinary KV alone would leave part of the model's history missing.

### Branch checkpoints

Suppose the host already holds Full KV through P3 and a recurrent checkpoint
S0 at P3. A new request branches at P2 and produces checkpoint S1 there.
The old S0 cannot serve that branch: it includes tokens after P2. If backup
only checks whether the node already has a host copy, it skips S1. The next
request can reload the Full KV but still cannot resume at P2 with that state.

SGLang adds an incremental backup for the new checkpoint. Full KV stays where
it is; only S1 needs another transfer. Backup now tracks newly available
components, rather than treating the entire prefix as already saved.

<StateRestoreViz client:visible lang="en" />

The recovery test creates branch states between two rounds of GPU eviction.
Its final stage checks whether each request recovers the full shared prefix.
The measured result is shown in the diagram. This change covers write-through
backup; it does not establish the same behavior for write-back.

## Draft-state recovery

Speculative decoding adds another consumer of the prefix. A draft model proposes
tokens for the target model to verify, and the draft can have its own KV or
other history buffers. Restoring target KV alone can leave draft state missing
or mapped to the wrong slots. A target cache hit can therefore coexist with a
drop in draft acceptance rate.

SGLang attaches matching draft buffers to the target's cache transfers. When
their layouts fit together, it packs the draft data into the corresponding
target host pools. Otherwise, separate sidecar pools follow the target's KV or
sliding-window indices. In both cases, backup and recovery carry the associated
draft state through L2 and L3 as well. This is the mechanism behind HiCache's
expanded MTP, EAGLE, and DSpark support.

<DraftRestoreViz client:visible lang="en" />

The DSpark test runs the same prompts cold and again from L3. After the change,
replay acceptance stays close to the cold run. The result shows why restoring
the complete state matters beyond prefill: it also preserves the draft's
effectiveness during subsequent decoding. Acceptance rate is not an
end-to-end throughput measurement.

## Host memory as a buffer

The original hierarchy uses CPU memory to retain a second cache. That is useful
when a prefix can be reloaded from L2 instead of fetched from storage. But each
serving instance owns its L2 pool. Several instances can retain separate host
copies while also backing up to a shared L3. Enlarging every L2 consumes more
host memory even when L3 already holds the reusable history.

Buffer-only mode changes the role of CPU memory. Backups pass through a host
buffer on their way to L3, and prefetches pass through it on their way to the
GPU. Once a transfer completes and releases its slots, another transfer can
use them. The host pool holds data in transit instead of retaining a second
working set. L3 sharing still requires compatible model configurations;
identical token text alone does not make two models' KV interchangeable.

<HostModeViz client:visible lang="en" />

The tradeoff is less persistent L2 reuse and potentially more L3 traffic.
A smaller host pool also limits how many transfers can be in flight.
Buffer-only therefore saves host capacity when storage can carry the reuse
load; it does not eliminate the CPU transfer path or guarantee lower latency.

## Results

### Long context · 2025

The original LooGLE benchmark uses DeepSeek-R1 on eight H20-3e GPUs. The chart
compares GPU-only caching, an added CPU L2, an added 3FS L3, and a separately
prepopulated L3. Switch the metric to compare average TTFT and input throughput.

<ResultsViz client:visible lang="en" />

The ordinary L3 result is 51,168 input tokens/s with 1.83-second average TTFT.
Prepopulating L3 produces 76,322 input tokens/s and 1.43 seconds under a different
warmup condition. The two L3 bars therefore use different warmup conditions.
Input throughput under cache reuse includes progress through cached input;
it is not the rate at which the GPU newly computes every token's KV.

### Hybrid multi-turn workloads · 2026

The August 2026 Unified Radix Cache evaluation compares GPU L1, L1 plus CPU L2,
and L1 plus L2 plus a 500 GiB Mooncake L3. It uses DeepSeek-V4-Flash with FULL and
SWA, and Inkling-Small with FULL, SWA, and MAMBA. The configurations and scales
differ between models, so compare tiers within each model.

<MultiTurnBenchViz client:visible lang="en" />

On DeepSeek-V4-Flash, effective input throughput increases from 9.4K to 145.5K
tokens/s with L3, and the hit rate stays near 98%. Inkling-Small reaches 67.1K
instead of 15.5K and ends at a 96.8% hit rate. These measurements include cached
input tokens in serving progress. They show the benefit of retaining growing
conversation histories, not a corresponding multiplication of raw GPU compute.

## Original blog acknowledgments

The original blog was written by Zhiqiang Xie. It credits the Alibaba Cloud
TairKVCache team for 3FS integration; contributors from Ant Group, Alibaba Cloud,
Approaching.AI, and the Mooncake community for Mooncake integration; and NVIDIA's
Dynamo team for NIXL. It also thanks contributors from SGLang, LMCache, NVIDIA,
and Stanford for the design and implementation, and the LMCache, AIBrix, PrisDB,
and ByteDance EIC teams for ecosystem work. The original article lists the
individual contributors.
