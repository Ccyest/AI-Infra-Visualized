---
title: "SGLang HiCache：支持混合模型，输入吞吐最高达 15.5 倍"
description: "SGLang 通过复用缓存前缀避免重复 prefill，但 GPU 显存无法保留所有不断增长的对话。HiCache 把缓存扩展到 CPU 内存和外部存储。要让新增容量产生收益，还需要高效搬运数据、权衡等待与重算，并完整恢复混合模型和投机解码所需的状态。本文从分层设计出发，逐项解释这些工程问题、对应实现与实际效果。在 DeepSeek-V4-Flash 多轮基准中，加入 Mooncake L3 后，有效输入吞吐达到仅 GPU 缓存的 **15.5 倍**，缓存命中率保持在约 **98%**。"
publishedDate: "2026-09-10"
updatedDate: "2026-09-14"
order: 8
concepts: []
tryOnSglang: "https://github.com/sgl-project/sglang"
references:
  - label: "LMSYS Blog：SGLang HiCache: Fast Hierarchical KV Caching with Your Favorite Storage Backends"
    url: "https://www.lmsys.org/blog/2025-09-10-sglang-hicache/"
  - label: "sglang#8651：Support page first layout  zero copy for mooncake store"
    url: "https://github.com/sgl-project/sglang/pull/8651"
  - label: "sglang#9109：3fs zerocopy"
    url: "https://github.com/sgl-project/sglang/pull/9109"
  - label: "SGLang Cookbook：HiCache"
    url: "https://sgl-project-sglang-93.mintlify.app/optimization/hicache"
  - label: "SGLang：HiCache 基准测试"
    url: "https://github.com/sgl-project/sglang/tree/main/benchmark/hicache"
  - label: "sglang#2693：Hierarchical Caching for SGLang"
    url: "https://github.com/sgl-project/sglang/pull/2693"
  - label: "sglang#7313：Kernels for efficient KV cache IO"
    url: "https://github.com/sgl-project/sglang/pull/7313"
  - label: "sglang#7704：Hicache Storage Layer Prototype"
    url: "https://github.com/sgl-project/sglang/pull/7704"
  - label: "sglang#21259：[HiCache & HybridModel] mooncake backend support DSA & mamba  model"
    url: "https://github.com/sgl-project/sglang/pull/21259"
  - label: "sglang#30393：[HiCache] Support packed and sidecar draft caches for MTP/EAGLE/DSpark"
    url: "https://github.com/sgl-project/sglang/pull/30393"
  - label: "sglang#33639：[Hicache][2/2]Support Mamba branching in Unified Radix Cache with HiCache"
    url: "https://github.com/sgl-project/sglang/pull/33639"
  - label: "sglang#34798：[HiCache] Buffer-only mode for HiCache host memory layer"
    url: "https://github.com/sgl-project/sglang/pull/34798"
  - label: "sglang#31920：[HiCache] Add model-aware key isolation to Mooncake Store"
    url: "https://github.com/sgl-project/sglang/pull/31920"
  - label: "sglang#37050：docs: state that HiCache L2 is instance-private and only L3 is shared"
    url: "https://github.com/sgl-project/sglang/pull/37050"
  - label: "LMSYS Blog: Unified Radix Cache: One Tree for Hybrid Model Prefix Caching"
    url: "https://www.lmsys.org/blog/2026-08-11-unified-radix-cache/"
---

import StorageLocationViz from "../../../viz/hicache/StorageLocationViz";
import CacheReuseViz from "../../../viz/hicache/CacheReuseViz";
import LayoutViz from "../../../viz/hicache/LayoutViz";
import OverlapViz from "../../../viz/hicache/OverlapViz";
import PrefetchViz from "../../../viz/hicache/PrefetchViz";
import ResultsViz from "../../../viz/hicache/ResultsViz";
import TimelineViz from "../../../viz/hicache/TimelineViz";
import StateRestoreViz from "../../../viz/hicache/StateRestoreViz";
import DraftRestoreViz from "../../../viz/hicache/DraftRestoreViz";
import HostModeViz from "../../../viz/hicache/HostModeViz";
import MultiTurnBenchViz from "../../../viz/unified-radix-cache/MultiTurnBenchViz";

## 背景

模型为读入的 token 计算 key 和 value，简称 KV。生成时，attention 通过这些 KV 读取前文。当另一个请求以相同的 token 前缀开头，SGLang 就能复用对应的 KV，只计算新增的后缀。这减少了 prefill 工作，也缩短了首 token 延迟，即 TTFT。

GPU 显存容量限制了这种复用。对话每一轮都在增长，更多客户端又带来更多历史。缓存满了以后，某段前缀可能刚被驱逐，下一个请求就需要它。SGLang 的 HiCache 用容量更大的存储层保留这些前缀。要让新增容量产生收益，恢复前缀必须比重新计算更划算，而且恢复的状态必须足以让模型从这个前缀继续执行。

HiCache 先接入 CPU 内存，再扩展到外部存储。后续工作把恢复范围扩展到混合模型和 draft 状态，并提出了让 host 内存只作传输缓冲的方案。时间线展示这些变化的背景，后文则沿着它们解决的工程问题展开。

<TimelineViz client:visible />

## HiCache

### 设计与机制

把所有可复用前缀都留在 GPU 上，会挤占活跃请求所需的显存。因此，HiCache 把前缀的保存位置与模型的使用位置分开：GPU HBM 是 L1，CPU DRAM 是 L2，外部存储是 L3。Attention 仍由 GPU 执行，较低层保留原本会被丢弃的数据，cache controller 负责备份和加载。

<StorageLocationViz client:visible />

假设两次请求都以同一份文档开头，只是后面的问题不同。SGLang 可以复用文档对应的 KV，只计算新问题。HiRadixTree 负责匹配相同的前缀，并记录本地缓存的位置；HiCache 负责在需要时把下层缓存搬回 GPU。

<CacheReuseViz client:visible />

L1 命中直接复用；L2 命中先搬回 L1；L3 命中先取到 L2，再搬回 L1。只有三层都找不到的部分才需要重新计算。HiCache 的收益就在这里：即使 GPU 清掉了某段缓存，只要下层还保留着，就有机会省掉重算。这要求两次请求的前缀 token 一致，仅仅意思相近不能复用。

### 工程问题纵览

分层设计提供了保留和恢复前缀的路径。要让这条路径真正有用，实现中还需要解决几类问题：

- **内存布局：** GPU 计算按模型层排列数据，存储 I/O 则按照page排列数据，怎样让不同的排列顺序之间仍然高效搬运？
- **重叠搬运与计算操作：** 哪些传输可以与 GPU 计算重叠，让GPU不空等？
- **缓存策略：** 预取和备份都消耗时间、带宽与容量，什么时候值得等待，什么时候值得多存一份？
- **混合模型与 draft 状态：** token 匹配到了，所需历史状态却可能不完整，哪些数据必须随前缀一起恢复？
- **Host 内存：** 每个实例都有私有 L2 池，L3 已经保留历史时，host 还需要长期保存多少数据？
- **存储后端：** 不同系统的 I/O 实现各异，怎样共用 HiCache 的调度与恢复机制？




















### 内存布局

这项优化解决 GPU 计算与存储 I/O 所需的 KV 排列顺序不同的问题。GPU 每次计算一个模型层，为了便于读取，就会把一层的KV放一起。而L3外部存储则按页读写（如第一页包含token 1-4，第二页包含 5-8）

因此，如果在Host段仍然不改变排列方式的话，如图所示，在搬运过程中就会有多次小的搬运开销。相反，如果Host段使用按页排列，则可以一次搬运一整页到L3外部存储，减少了搬运次数与开销。

**因此，HiCache 让 Host 按页排列，作为中转站。** HiCache 让 Host 在搬运时转换布局。如图所示。图中高亮第 1 页：Before 里，它分散在 Host 的多个区域；After 里，它集中成一块。Host → 存储备份时，可以直接读取这一整块；存储 → Host 恢复时，可以直接写入这一整块，减少开销。



<LayoutViz client:visible />

**GPU ↔ Host** 这一段，SGLang自己研发的GPU 辅助的 I/O kernel 让传输吞吐最高达到标准 `cudaMemcpyAsync` 路径的 **3 倍**。**Host ↔ 存储** 这一段，按页排列让存储能读写连续的大块数据；zero-copy 让存储客户端直接访问 Host 池里的这些数据。按页排列与 zero-copy 配合，让 Host–存储吞吐最高达到 **2 倍**。

### 重叠搬运与计算操作

前文讲了搬运如何加速，这项优化则是把 **Host → GPU 搬运缓存 KV** 与 **GPU 计算新 token** 重叠起来。在搬运过程中，我们不想要GPU空等，理想状态是每搬运一点GPU就算一点。因此，需要 HiCache 在第一个模型层的 KV 到齐后就开始计算，同时继续搬运后续模型层的 KV。

以三层模型为例：先把文档在第 0 层的 KV 从 Host 搬到 GPU。GPU 随即对新问题执行第 0 层计算，并通过这份缓存 KV 读取文档内容。与此同时，传输流搬运文档在第 1、2 层的 KV。

能这样重叠，是因为每个模型层使用自己的 KV。第 0 层计算时，不需要等第 1 层的 KV。计算走到第 1 层时，再检查第 1 层是否搬完：搬完就直接计算，没搬完才等这一层。L2 命中可以直接开始这个 Host → GPU 过程；L3 命中则先把 KV 从存储读到 Host，再进行同样的重叠操作。

<OverlapViz client:visible />



### 缓存策略

缓存策略决定什么时候保存 KV，以及什么时候值得等它。备份把 GPU 上的 KV 复制到 Host，供后续请求复用；预取则在请求执行前，把 L3 中保存的 KV 读到 Host。多存一份，后续就多一次命中的机会，但现在要花带宽、占内存；多等一会儿，可能省去重算，但请求也会更晚开始。

#### 预取

假设 GPU 正在处理请求 A，此时请求 B 到来，B 所需的文档 KV 保存在 L3。HiCache 可以在 B 等待调度时，提前把它的 KV 读到 Host，让这段读取与 A 的计算同时进行。等轮到 B 时，就能把已经到达 Host 的 KV 搬上 GPU，省去临时读取存储的等待。

如果轮到 B 执行时，L3 还没读完，就由预取策略决定下一步。Best-effort 不再等，复用已经就绪的前缀，剩下的重算；wait-complete 继续等待读取完成；timeout 给读取额外时间，到达等待上限后，使用已经就绪的前缀开始执行。

<PrefetchViz client:visible />

继续等是否划算，要比较两件事：把缺失 KV 读回来要多久，把这部分重新算出来要多久。文档很长时，多等一小会儿可能省下大量 prefill；存储读取很慢时，直接重算反而更早结束。预取争取在请求执行前读完，预取策略则处理届时还没读完的部分。

#### 备份

后续请求能命中 Host，前提是此前保存过副本。GPU 算好一份文档的 KV 后，HiCache 可以把它备份到 Host；启用 L3 后，还能进一步保存到存储。但如果每段缓存都备份，那些以后再也没人问的文档，也会消耗搬运带宽和缓存容量。

对于 GPU → Host 备份，HiCache 提供三种选择：

| 策略 | 备份时机 | 主要取舍 |
| --- | --- | --- |
| Write-through | KV 进入缓存后，主动备份到 Host | 保留更多前缀，也产生更多备份流量 |
| Write-through-selective | 根据命中次数，挑选热点前缀备份到 Host | 减少给冷门前缀花的带宽 |
| Write-back | 等 KV 要从 GPU 驱逐时，再备份到 Host | 推迟复制，但驱逐时要多做一次备份 |

一份文档会反复被请求时，提前备份能让 GPU 副本被驱逐后，Host 仍有数据可用。选择性备份优先照顾这些热点文档，减少一次性输入的备份。Write-back 则先把 KV 留在 GPU，等到需要驱逐时才复制，把备份开销推迟到那一刻。

### 混合模型的状态

对于混合模型，匹配到同一份文档的 token 还不够。同一个模型里可以同时有全注意力层、滑窗注意力层和递归层，它们保存历史的方式不同。只恢复全注意力的 KV，其他层可能仍然缺少从文档末尾继续计算所需的数据。

#### 复用边界

复用边界就是缓存前缀结束、新计算开始的位置。要从这里继续，全注意力需要前缀的 KV；滑窗注意力需要边界前窗口内的 KV；递归层需要恰好在这个位置保存的状态快照，也叫 checkpoint。这个快照记录了递归层读完该前缀时的状态。更晚的快照已经包含后续 token，删掉几页 KV 并不能把它还原成更早的状态。

SGLang 的 Unified Radix Cache 把这些不同的数据关联到同一个 token 前缀，检查某个位置需要的状态是否齐全；HiCache 再把对应数据恢复到 GPU。除了注意力 KV，还要一起恢复配套数据，例如 DeepSeek-V3.2 稀疏注意力使用的 indexer buffer。

例如，文档的全注意力 KV 都在，但文档末尾的递归快照丢了，请求就不能直接跳到新问题开始计算。它需要从更早、状态齐全的位置继续，重算缺失的部分。把 checkpoint 和 KV 一起保存，才能保住从文档末尾继续执行的机会。

#### 分支 checkpoint

新分支可能需要初次备份时还不存在的 checkpoint。图中 Host 已保存直到 P3 的全注意力 KV，以及 P3 处的快照 S0。后来一个请求在更早的 P2 处分支，并在那里计算出快照 S1。S0 已经包含 P2 到 P3 之间的 token，分支需要的是 S1。如果备份流程看到这个节点已经有 Host KV 就跳过，新增的 S1 就会漏存。

SGLang 在 write-through 路径上增加了 checkpoint 增量备份：Host 已有的全注意力 KV 保留原样，把新生成的 S1 单独补存进去。这样，S1 从 GPU 驱逐后，后续请求仍能从 Host 恢复它，并从 P2 继续执行。

<StateRestoreViz client:visible />

恢复测试采用 write-through 策略：先生成新分支的快照，再把它们从 GPU 驱逐，最后重新请求共享前缀。完整恢复 6.4K 共享前缀的请求从 **1/10 增至 10/10**，token 命中率从 **15.74% 增至 64.00%**。每个请求包含 6.4K 共享 token 和 3.6K 新 token，因此 64% 表示共享前缀全部恢复。详见[分支备份测试](https://github.com/sgl-project/sglang/pull/33639)。

### Draft 状态恢复

投机解码由 draft 模型先提出候选 token，再由 target 模型验证。对于同一份文档，draft 也可能保存自己的 KV 和其他历史数据。如果缓存命中后只恢复 target 的数据，draft 可能已经丢失历史，或者去错误的内存位置读取历史。这样，即使 target 成功复用了前缀，draft 提出的 token 也可能更难通过验证。

因此，HiCache 在备份和恢复 target 状态时，也带上对应的 draft 状态。布局兼容时，把 draft 数据打包进对应的 target Host 池；其他布局则单独分配一个 sidecar 池，让它的槽位跟随 target 的 KV 或滑窗槽位。这样，经过 L2、L3 搬运后，仍能找到每段前缀对应的 draft 数据。

<DraftRestoreViz client:visible />

DSpark 测试在 DeepSeek-V4-Flash-0731 上运行同一批 200 个 prompt，先做冷运行，再从 L3 恢复后运行。修复前，恢复后的 draft 接受率从 **46.77% 降至 33.57%**；修复后，两次运行分别为 **46.12% 和 46.86%**。恢复 draft 的历史后，缓存命中也能保住候选 token 的有效性。详见 [draft 恢复验证](https://github.com/sgl-project/sglang/pull/30393)。

### Host 内存作为缓冲

把文档 KV 留在 Host，下一次请求就能直接命中 L2。但每个服务实例都有自己的 Host 池。图中的实例 A、B 可以各自在 Host 留一份前缀 P，共享 L3 也留着一份。保存的文档越多，每个实例占用的 Host 内存就越多，即使这些文档在 L3 中已经有副本。

Buffer-only 模式让 Host 只在搬运期间保存数据。备份时，A 把 P 从 GPU 搬到 Host，再写入 L3；传输完成、不再需要这块缓冲后，就可以复用这些 Host 槽位。恢复时，B 把 P 从 L3 读到自己的 Host 缓冲，再搬到 GPU，随后释放缓冲。P 留在 L3 供以后复用，A、B 的 Host 槽位则可以接着搬其他数据。

<HostModeViz client:visible />

这样，Host 只需容纳正在搬运的数据，不必长期保存一整批文档。代价是下一次 GPU 未命中时，要重新读取 L3，无法命中长期保留的 Host 副本；Host 缓冲越小，也能同时容纳越少的搬运任务。Buffer-only 用更多的 L3 读取，换取更少的 Host 副本占用。

### 存储后端

假设部署把 L3 从本地文件换成 Mooncake，KV 的读写就从文件操作变成了访问存储服务。但 HiCache 仍然要查询缓存、发起预取、安排 Host → GPU 加载，并在使用数据前等待搬运完成。这些工作不应该每换一个存储后端就重写一遍。

HiCache 把存储访问与缓存调度分开。后端实现怎样查询一个 key 是否存在、怎样把数据读到 Host，以及怎样把 Host 数据写入存储；共用的 cache controller 决定什么时候发起这些操作，并协调完成时机。Mooncake、DeepSeek 3FS 和本地文件都通过这套接口接入；NIXL 则通过传输库连接存储系统。

因此，接入新后端时，只需实现对应的存储访问，就能沿用 HiCache 已有的预取、备份和 GPU 加载流程。部署可以更换 KV 的保存位置，同时保留同一套缓存调度。

## 整体效果

长输入和多轮对话的历史会超过 GPU 显存容量。下面的基准依次加入 CPU 内存和外部存储，观察保留更多历史之后，首 token 延迟和输入吞吐怎样变化。

### 长上下文 · 2025

原文的 LooGLE 测试验证较低层缓存能否减少长输入的重复 prefill。它在八张 H20-3e 上运行 DeepSeek-R1，比较仅 GPU 缓存、增加 CPU L2、增加 3FS L3，以及单独预先填充 L3 的情况。图中可以查看平均 TTFT 和输入吞吐。

<ResultsViz client:visible />

普通 L3 的输入吞吐为 51,168 tokens/s，平均 TTFT 为 1.83 秒。如果在请求到来之前，就把可复用的 KV 填入 L3，更多请求可以直接加载缓存前缀。输入吞吐提高到 76,322 tokens/s，平均 TTFT 降至 1.43 秒。

### 混合模型多轮对话 · 2026

多轮对话提供了不断增长的工作集：每一轮都新增后续请求可能复用的历史。2026 年 8 月的测试比较 GPU L1、L1 加 CPU L2，以及再加 500 GiB Mooncake L3。DeepSeek-V4-Flash 使用 FULL 与 SWA，Inkling-Small 使用 FULL、SWA 与 MAMBA。

<MultiTurnBenchViz client:visible />

L1 最先丢失可复用历史，L2 推迟了容量上限出现的时刻。相比仅 L1，增加 L3 后，DeepSeek-V4-Flash 的有效输入吞吐从 9.4K 增至 145.5K tokens/s，缓存命中率保持在约 98%。Inkling-Small 从 15.5K 增至 67.1K，最终命中率为 96.8%。结果表明，当混合模型的对话历史超过较小缓存的容量时，较低层仍能保住复用机会。详见[多轮基准](https://www.lmsys.org/blog/2026-08-11-unified-radix-cache/)。

输入吞吐统计服务每秒处理的所有输入 token，包括命中缓存的部分。复用一段长前缀，就能省去这段前缀的 KV 重算，把更多 GPU 时间留给新增输入。

## 总结

HiCache 把可复用前缀的保存时间延长到了 GPU 显存之外。各项工程选择让新增容量真正可用：布局和加载方式减少搬运开销，策略控制何时值得做 I/O，组件与 draft 恢复保住所需的完整状态，host 缓冲与后端选择则让同一套分层机制适应不同部署资源。

当省下的 prefill 工作超过恢复成本，而且较低层保住了原本会丢失的历史，系统就能获得整体收益。长输入和不断增长的对话基准验证了这种效果。

## 原博客致谢

原博客由 Zhiqiang Xie 撰写。文中感谢阿里云 TairKVCache 团队完成 3FS 集成；蚂蚁集团、阿里云、Approaching.AI 和 Mooncake 社区的贡献者推进 Mooncake 集成；NVIDIA Dynamo 团队完成 NIXL 集成。同时感谢 SGLang、LMCache、NVIDIA 和 Stanford 的贡献者参与设计与实现，以及 LMCache、AIBrix、PrisDB 和字节跳动 EIC 团队的生态工作。完整个人名单见原文。
